import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0896A6",
    },
    secondary: {
      main: "#0889A6",
    },
    background: {
      default: "#8AA3A6",
    },
  },
});

export default theme;
